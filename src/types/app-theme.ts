export type ThemeAppTypes =
  | "dark-theme"
  | "midnight-theme"
  | "pastel-theme"
  | "default-theme";

export const APP_THEME_CLASSES = [
  "dark-theme",
  "midnight-theme",
  "pastel-theme",
  "default-theme",
] as const;
